# stratalint-cli

This package supplies the native `strata` executable. Install `stratalint` for
the Python authoring API, native extension, command fallback, and this binary as
one locked dependency set.
